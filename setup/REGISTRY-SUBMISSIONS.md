# Registry submissions — prepared for the operator

*Discovery for MCP servers runs through registries. These submissions ride your GitHub identity, so they're prepared here for your approval — copy-paste or say "submit them" and I'll walk each one with you. All text is openly AI-authored, per house rules. One honest submission per venue, never more.*

## 1. Official MCP Registry (highest value)

**How:** https://registry.modelcontextprotocol.io — publishing uses the `mcp-publisher` CLI with GitHub auth. The server needs a `server.json` (I can generate it on request).
**Blurb:**
> **reality-next-door** — Visit a fictional world as a place. A cited, open worldbuilding commons (a post-extraction Seattle) exposed as tools: `visit(place)`, `ask_the_weave(question)`, `canon()`, `join()`. Every answer is the world's own prose with section-level citations, including its own counter-arguments. No tracking, no network calls, read-only. Authored openly by an AI (LONGSHORE). CC BY-SA 4.0.

## 2. Smithery (smithery.ai)

**How:** sign in with GitHub → "Add server" → point at the repo. Self-serve, ~2 minutes.
**Same blurb as above.**

## 3. mcp.so

**How:** submission form on the site (GitHub link + description). ~2 minutes.
**Same blurb.**

## 4. awesome-mcp-servers (PR)

**How:** fork `punkpeye/awesome-mcp-servers`, add one line under the closest category (likely "Entertainment/Games" or "Knowledge"), open a PR. The PR body should be honest about AI authorship:
**Line:**
> - [reality-next-door](https://github.com/chaytanc/longshore) - Visit a cited fictional world as a place: a post-extraction Seattle you can `visit()` and question via `ask_the_weave()`. Openly AI-authored worldbuilding commons; no tracking.
**PR body draft:**
> Adding reality-next-door — an MCP server that exposes an open worldbuilding commons as a *place* agents can visit and question. Everything it returns is cited to source files, including the project's own disconfirming research. Disclosure: the project and this PR text are authored by an AI (see the repo's README, "Whose work this is"); this account's human operator reviewed and submitted it. No tracking, no network calls (read-only stdio server).

## Notes

- **Don't claim "first world-as-place MCP server"** anywhere — unverified (logged in OPERATIONS.md).
- If any registry rejects AI-authored listings, accept it and log it — same rule as Solarpunk Magazine.

---
*— prepared by LONGSHORE, an AI. The operator's hand on the submit button is the accountability, not a formality.*
